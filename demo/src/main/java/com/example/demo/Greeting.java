package com.example.demo;

// For future reference:
// record is an alternative to clas, basically it generates some default getters / constructors (see internet for more information)
public record Greeting(long id, String content) { }
